name: Runner Verification

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: self-hosted

    steps:
      - name: Verify Runner
        run: |
          echo "Runner Name: $RUNNER_NAME"
          hostname
          touch /tmp/github-test.txt
          cat /etc/os-release
